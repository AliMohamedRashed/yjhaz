<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AdvancedTask" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.AdvancedTask" parent="Base.Theme.AdvancedTask" />
    <style name="button_style" parent="Widget.MaterialComponents.Button">
    <item name="android:layout_height">@dimen/_44sdp</item>
    <item name="backgroundTint">@color/button_color</item>
    <item name="background">@drawable/custom_button</item>
    <item name="cornerRadius">@dimen/_10sdp</item>
    <item name="android:textAllCaps">false</item>
    <item name="android:textColor">@color/white</item>
    <item name="android:textAlignment">center</item>
    <item name="android:fontFamily">@font/tt_commons_regular</item>
    <item name="android:paddingBottom">@dimen/_5sdp</item>
    </style>
</resources>